# Where /api/* is proxied (the .NET API). Server-side only.
API_ORIGIN=http://localhost:5080

# client (inlined at build time)
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000
APP_ENV=local
