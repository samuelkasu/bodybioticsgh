# loaded by next/jest
# absolute, because Node Request rejects relative URLs and RTK Query builds one
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000
API_ORIGIN=http://localhost:5080
APP_ENV=local
