# Root-level only. Each workspace keeps its own ignore rules:
#   frontend/.gitignore  (Next, Jest, Playwright, Prisma output)
#   backend/.gitignore   (bin, obj, user settings)

# os / editor
.DS_Store
Thumbs.db
.idea/
.vs/

# env files (can opt-in for committing if needed)
.env
.env.*
!.env.example
!.env.test

# deployment
.vercel

# local machine config: holds a real database password
backend/src/BodyBiotics.Api/appsettings.Development.json

# Throwaway output from the WooCommerce cutover: sitemap scrapes, URL diffs and
# the lists the reconcile/recover scripts print. Regenerated by rerunning them;
# `docs/CUTOVER-404S.md` is the written-up conclusion and is committed.
.cut/
.cutover/
